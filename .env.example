# ==============================================
# ActivityBar Environment Variables
# ==============================================
# Copy this to .env.local and fill in your values
# Then run: source .env.local

# ----------------------------------------------
# GitLab (OAuth only)
# ----------------------------------------------
# OAuth app credentials (required for OAuth flow)
# Register at: https://your-gitlab.com/-/profile/applications
# Redirect URI: http://127.0.0.1:8765/callback
# Scopes: read_api, read_user
ACTIVITYBAR_GITLAB_CLIENT_ID=your_gitlab_application_id
ACTIVITYBAR_GITLAB_CLIENT_SECRET=your_gitlab_secret

# Default self-hosted URL (UI prefill - optional)
# Leave empty or omit for gitlab.com
ACTIVITYBAR_DEFAULT_GITLAB_HOST=gitlab.yourcompany.com

# ----------------------------------------------
# Azure DevOps (PAT only - no OAuth)
# ----------------------------------------------
# Personal Access Token (UI prefill - optional)
# Generate at: Azure DevOps → User Settings → Personal access tokens
ACTIVITYBAR_AZURE_PAT=your_azure_devops_pat

# Organization name (UI prefill - optional)
ACTIVITYBAR_AZURE_ORGANISATION=your_organisation

# Projects list, comma-separated (UI prefill - optional)
ACTIVITYBAR_AZURE_PROJECTS=Project1,Project2

# ----------------------------------------------
# Google Calendar (OAuth only)
# ----------------------------------------------
# OAuth app credentials (required for OAuth flow)
# Register at: https://console.cloud.google.com/apis/credentials
# Redirect URI: http://127.0.0.1:8765/callback
ACTIVITYBAR_GOOGLE_CLIENT_ID=your_client_id.apps.googleusercontent.com
ACTIVITYBAR_GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxxxxxxx
